version: 2.1
orbs:
  cypress: cypress-io/cypress@2.0.0
workflows:
    example-workflow:
      jobs:
        - cypress/run:
            record: true 
            parallel: true 
            parallelism: 4 
